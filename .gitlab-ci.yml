stages:
  - deploy

deploy:
  stage: deploy
  script:
    - cd /home/gitlab-runner/p_rozproszone

    - python3 -m venv .venv
    - source .venv/bin/activate

    - pip install -r requirements.txt

    - nohup python server.py > app.log 2>&1 &
